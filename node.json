{
    "extends": ["./base.json"],
    "compilerOptions": {
        "module": "nodenext",
        "moduleResolution": "nodenext",
        "baseUrl": "${configDir}",
        "paths": {
            "@/*": ["${configDir}/src/*"]
        }
    }
}
